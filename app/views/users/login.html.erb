<% content_for :title, 'Login' %>

<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 main">
      <% if flash[:error] %>
        <p class="alert alert-danger"><%= flash[:error]%></p>
      <% end %>

      <h1>Login</h1>

      <%= form_for(:user, url: {action: :post_login, previous_url: @previous_url}) do |f| %>
        <div class="form-group">
          <%= f.label "Email address" %>
          <%= f.text_field :username, autocomplete: "off", autofocus: "autofocus", class: "form-control" %><br />
        </div>

        <div class="form-group">
          <%= f.label :password %>
          <%= f.password_field :password, class: 'form-control' %><br />
          <%= f.submit 'Login', class: 'btn btn-large btn-primary' %>
        </div>

        <div class="form-group">
          <a href="/admin/users/reset_password_page">Reset Password</a>
        </div>
      <% end %>
    </div>
  </div>
</div>
